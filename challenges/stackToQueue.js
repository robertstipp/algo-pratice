// implement a stack using queues as the underlying data structure
// for example, let stack = new stackToQueue()
// stackToQueue.push(1) => undefined
// stackToQueue.pop() => 1
// stackToQueue.push(1) => undefined
// stackToQueue.push(2) => undefined
// stackToQueue.pop() => 2
// stackToQueue.pop() => 1
// stackToQueue.pop() => undefined
// REMEMBER: That a stack uses Last-In First-Out(LIFO) order, while queues maintain a First-in First-Out(FIFO) order
// https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
// https://en.wikipedia.org/wiki/Queue_(abstract_data_type)

// How can you intertwine the functionality of two queues such that they maintain a stack order?

class stackToQueue {
  constructor() {}
}

module.exports = { stackToQueue };
